return {
    cmd = {
        "clangd",
        -- This is how you specify a query driver for clangd.
        -- "--query-driver=C:\\msys64\\mingw64\\bin\\gcc.exe",
        -- Setting log level.
        -- "--log=verbose",
    }
}
